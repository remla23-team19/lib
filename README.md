# lib package for team 19 (REMLA 2023)

This is a version aware lib package on which the front-end depends.

how to install:
```console
$ python3 -m pip install --index-url https://test.pypi.org/simple/ --no-deps lib-remla19
```
